#class {buy} {open}
  #alias {buyerbs} {
    #variable cnt %1;
    #while {$cnt > 0} {
      buy erb;
      #math {cnt} {$cnt - 1}
    }
  }

  #alias {buymunitions} {
    #variable cnt %1;
    #while {$cnt > 0} {
      buyammo %2;
      #math {cnt} {$cnt - 1}
    }
  }
#class {buy} {close}
